/*---------------------------
 * c_share.h
 *  Created on: Jun 23, 2017
 *      Author: Bailin Rui
 *          ���ߣ��ǰ���
 *---------------------------
 *
 *
 *
 *
 *---------------------------
 */


#ifndef SW_CVE_3OPR_C_SHARE_H_
#define SW_CVE_3OPR_C_SHARE_H_

#include "../../code/sw_cve/2blk/sw_knl_base.h"
#include "../../ive_type.h"

#define MAX(a, b)			((a) > (b) ? (a) : (b))
#define MIN(a, b)			((a) < (b) ? (a) : (b))
#define ABS(x)				((x) > 0 ? (x) : -(x))

#endif 
/* end of c_share.h */
